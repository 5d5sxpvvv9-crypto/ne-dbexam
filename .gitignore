# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.*

# Backend temp (uploads / generated xlsx)
backend/temp_uploads/
backend/temp_outputs/

# OS / IDE
.DS_Store
.idea/
.vscode/

# 로컬 시험 데이터(용량·저작권) — 필요 시 `git add -f exam_dataset/파일` 로 개별 추가
/exam_dataset/
/*.hwp
/*.xlsx

# 이전 하위 저장소 백업(용량 큼 — 커밋하지 않음)
backend/.git_backup_*/
frontend/.git_backup_*/
